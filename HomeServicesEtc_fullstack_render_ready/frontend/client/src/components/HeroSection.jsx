import React from 'react';
import './HeroSection.css';

const HeroSection = () => (
  <section className="hero">
    <h2>Your Trusted Home Services Marketplace</h2>
    <p>Find and hire verified professionals in minutes.</p>
  </section>
);

export default HeroSection;
